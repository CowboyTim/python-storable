# Perl can "bless" many object types. In this particular case, a string was
# used. We can compare with a string object from Python, but we loose the
# package name (Aa::Bb in this case). I've decided that, for now, this is good
# enough.
result = 'Test'
